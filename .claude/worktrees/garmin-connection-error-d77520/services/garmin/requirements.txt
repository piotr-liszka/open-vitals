# Garmin sidecar runtime + test dependencies (Python 3.12 in Docker).
# Pins are lower-bounds; the image builds latest compatible at build time.
fastapi>=0.110
uvicorn[standard]>=0.29
garmy>=0.1
curl_cffi>=0.6
cryptography>=42
psycopg[binary]>=3.1
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
pytest>=8.0
